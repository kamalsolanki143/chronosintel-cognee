fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0

# Database — async SQLAlchemy
sqlalchemy>=2.0.0
aiosqlite>=0.20.0          # SQLite async driver (dev)
# asyncpg>=0.29.0          # Uncomment for PostgreSQL production
# psycopg2-binary>=2.9.0   # Uncomment for PostgreSQL (sync fallback)

# File handling
python-multipart>=0.0.9    # multipart/form-data uploads
aiofiles>=23.2.1            # async file I/O

# Document parsing
pypdf>=4.2.0               # PDF extraction
python-docx>=1.1.0         # DOCX extraction
openpyxl>=3.1.0            # Excel/XLSX
python-magic>=0.4.27       # MIME type detection (optional, may need libmagic)

# AI — Cognee Knowledge Graph
cognee>=0.1.0

# AI — Google Gemini
google-generativeai>=0.8.0

# Utilities
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0   # JWT (future auth)
httpx>=0.27.0                       # async HTTP client
tenacity>=8.2.0                     # retry logic

# Date / Time
python-dateutil>=2.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # async test client
