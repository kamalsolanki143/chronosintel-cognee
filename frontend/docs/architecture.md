# Architecture

ChronosIntel is split into a React frontend, FastAPI backend, and AI layer for Cognee memory and Gemini reasoning. The backend owns ingestion, persistence, evidence chains, timelines, reporting, and case memory. The AI layer handles graph memory, temporal search, entity extraction, grounded reasoning, and prompt templates.
