# Deployment

Create a `.env` file from `.env.example`, then run:

```bash
docker compose up --build
```

For production, configure persistent storage, managed secrets, HTTPS, and a production database.
